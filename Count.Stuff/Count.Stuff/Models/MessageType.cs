
namespace Count.Stuff.Models
{
    /// <summary>
    /// Message type that will determine what handler need to do the work
    /// </summary>
    enum MessageType
    {
        BeginProcess,
        SaveDetails
    }
}
